<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/linearLayout"
    android:layout_width="match_parent"
    android:layout_height="wrap_content">

    <TextView
        android:id="@+id/event_list_event"
        android:layout_width="wrap_content"
        android:layout_height="27dp"
        android:text="eventlist"
        app:layout_constraintStart_toEndOf="@+id/marker_image"
        app:layout_constraintTop_toTopOf="parent" />

    <TextView
        android:id="@+id/event_person_name"
        android:layout_width="wrap_content"
        android:layout_height="0dp"
        android:text="personName"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toEndOf="@+id/marker_image"
        app:layout_constraintTop_toBottomOf="@+id/event_list_event" />

    <ImageView
        android:id="@+id/marker_image"
        android:layout_width="55dp"
        android:layout_height="55dp"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="@+id/event_list_event" />

</android.support.constraint.ConstraintLayout>